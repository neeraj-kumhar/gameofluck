{
  "name": "gameofluck-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../backend && npm install",
    "client-dev": "cd client && npm run dev",
    "backend-dev": "cd backend && npm run dev",
    "build": "cd client && npm install && npm run build && cd .. && rm -rf dist && cp -r client/dist ./dist",
    "dev": "npm run backend-dev & npm run client-dev"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.4.1",
    "mysql2": "3.3.3",
    "sequelize": "^6.37.8",
    "sequelize-cli": "^6.6.2",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": "20.x"
  }
}
