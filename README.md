# Game of Luck
Full-stack gaming platform with MySQL backend and React frontend.

## Setup
1. Backend: `cd backend && npm install && npm run dev`
2. Frontend: `cd client && npm install && npm run dev`
